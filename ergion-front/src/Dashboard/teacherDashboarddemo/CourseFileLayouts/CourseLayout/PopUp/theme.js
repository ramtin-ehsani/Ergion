import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  typography: {
    body1: {
      fontFamily: "IRANSans",
      fontWeight: 400,
      fontSize: 16,
      color: "red"
    }
  }
})

export default theme